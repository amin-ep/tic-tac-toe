import styled from "styled-components";

const Form = styled.form`
  display: grid;
  grid-template-columns: auto;
  grid-row-gap: 1.5rem;
  width: 100%;

  max-width: 100%;
  padding: 1rem;
`;

export default Form;
